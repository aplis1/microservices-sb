spring:
  application:
    name: "configserver"
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: "classpath:/config"
          # search-locations: "file:///Users//silpaaleti//Downloads//msbytes//config"
server:
  port: 8071
